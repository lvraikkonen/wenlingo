# V0.6c Deployed Targeted Manual QA

This checklist focuses on V0.6c newly deployed behavior, not a full-site regression.

## Environment

- Date: 2026-06-30
- Tester: Claus + Codex
- Web URL: `https://wenlingo.com`
- Railway deployed commit: `50d01b3` (local HEAD; not independently verified in Railway UI during this run)
- Browser: Chrome
- Test parent / child alias: Alpha parent session; QA aliases only: `QA v0.6c`, `QA v0.6c 景物`, `QA v0.6c 观察`, `QA v0.6c 新编`, `QA v0.6c 未支持`, `QA v0.6c AI`, `QA v0.6c 自画像`, `QA v0.6c 直写`. Do not record full email, verification code, or child's real essay text.

## Status Legend

- `[ ]` Not tested
- `[x]` Pass
- `[~]` Partial / follow-up needed
- `[!]` Fail
- `[n/a]` Not applicable / not safely triggered

## 0. Deploy Sanity

- [x] Railway Web/API deploy both reachable through deployed site flows
- [~] Opened site appears to be latest deployment; Railway UI commit was not independently checked
- [x] Can log in with Alpha parent account
- [x] Can enter child Dashboard
- [x] Can enter "作文城堡"
- [x] No obvious 500 / blank screen / hydration error during tested flows

Notes:

- Existing QA child aliases had in-progress writing states, so new QA child aliases were created for clean V0.6c targeted checks.

## 1. 新题型族：课内同步作文

Goal: Confirm the 4 new V0.6c topic families can be selected and enter the existing prewriting flow.

For each case, walk only to "第 1 步 / 共 4 步：看懂题目"; no need to complete a full essay.

### Practical Writing / 实用文

- Topic: `给老师写信`
- Expected: Can choose "写实用文" and enter Step 1; does not show "暂不支持题型".
- Result: [x]
- Notes:
  - Alias `QA v0.6c`; selected `写实用文`; entered `第 1 步 / 共 4 步：看懂题目`.

### Place Scenery / 写景

- Topic: `我的乐园` or `推荐一个好地方`
- Expected: Can choose scenery/place-related type and enter Step 1.
- Result: [x]
- Notes:
  - Alias `QA v0.6c 景物`; topic `我的乐园`; selected `写一处景物`; entered Step 1 after a slower generation wait.

### Animal/Object Observation / 观察类

- Topic: `我的植物朋友` or `我的心爱之物`
- Expected: Can choose observation-related type and enter Step 1.
- Result: [x]
- Notes:
  - Alias `QA v0.6c 观察`; topic `我的植物朋友`; selected `观察一种动物、植物或物品`; entered Step 1.

### Story Adaptation / 故事新编

- Topic: `续写故事` or `故事新编`
- Expected: Can choose story adaptation/continuation-related type and enter Step 1.
- Result: [x]
- Notes:
  - Alias `QA v0.6c 新编`; topic `续写故事`; selected `改编一个故事`; entered Step 1.

### Backlog Unsupported Remains Unsupported

- Topic: one of `缩写故事` / `推荐一本书` / `围绕中心意思写`
- Expected: Still shows unsupported/future-topic messaging; should not be accidentally allowed as a V0.6c supported type.
- Result: [x]
- Notes:
  - Alias `QA v0.6c 未支持`; topic `缩写故事`; did not enter Step 1. Page showed: `这类题目我们还在学习中，可以先选最接近的一种写法...`

## 2. AI 出题作文 Thin Slice

Goal: Verify interest -> exactly 3 ideas -> child selects -> existing prewriting flow.

- [x] Click "AI 出题作文"
- [x] In "兴趣或想写的方向", enter:

  ```text
  足球
  ```

- [x] Click "生成题目灵感"
- [x] Page displays exactly 3 topic ideas
- [x] Topic ideas are not complete essay paragraphs
- [x] Each idea reads like a topic/writing direction, not a decided concrete experience, ending, or feeling on behalf of the child
- [x] Click the first "选择这个题目"
- [x] Successfully enters "第 1 步 / 共 4 步：看懂题目"
- [x] Switching to another mode and back to AI 出题 should not repeat the selection page or regenerate the same flow unexpectedly

Notes:

- Alias `QA v0.6c AI`; interest `足球`; generated exactly 3 ideas: `一次难忘的足球赛`, `我的足球朋友`, `我的足球`.
- Selecting the first idea entered Step 1. Switching to `直接写初稿` and back to `AI 出题作文` returned to the selected topic's Step 1, not the idea cards.

## 3. AI 出题失败态

Only test in safe staging/dev conditions; do not change production provider configuration for QA.

- [n/a] If AI topic generation fails, child sees a friendly retry/later message
- [n/a] Failure does not show hidden fallback topic cards
- [n/a] Failure does not create an Essay
- [n/a] User can still switch back to "课内同步作文" or "直接写初稿"

If failure state cannot be safely triggered, record:

```text
Not manually triggered; covered by automated tests
```

Result: [n/a]

Notes:

- Not manually triggered; covered by automated tests. Production provider configuration was not changed for QA.

## 4. 家长 Summary

Goal: Confirm parent can see topic origin and AI did not generate copyable essay body.

### AI 出题作文 Path

Create one essay through the AI topic path, then inspect parent summary.

- [x] Shows "作文构思过程"
- [x] Shows:

  ```text
  题目来源：AI 出题灵感，孩子选择
  ```

- [x] Shows:

  ```text
  AI 正文：没有生成可直接照抄的作文正文
  ```

- [x] Essay type displays reasonably, such as 写景 / 实用文 / 想象作文
- [x] Does not show a complete AI-generated essay body

Notes:

- Alias `QA v0.6c AI`; summary showed topic `一次难忘的足球赛`, origin `AI 出题灵感，孩子选择`, type `写一件事`, and no copyable AI body.

### 课内同步作文 Path

Create one essay through the school-assigned topic path, then inspect parent summary.

- [x] Summary shows:

  ```text
  题目来源：老师布置题目
  ```

- [x] Still shows AI body was not generated
- [x] Existing prewriting process fields still display normally

Notes:

- Alias `QA v0.6c`; summary showed topic `给老师写信`, origin `老师布置题目`, type `写实用文`, and normal prewriting fields.

## 5. 旧路径回归

Goal: Confirm V0.6c did not break the existing usable learning loop.

- [x] "直接写初稿" can still submit and receive "修改小任务"
- [x] Existing topic type still works through school-assigned topic path:

  ```text
  我的“自画像”
  ```

- [x] Writing Castle mode switching works: 课内同步 / AI 出题 / 直接写初稿
- [x] After refreshing the page, an in-progress school-assigned essay does not lose progress
- [x] Parent summary page opens without errors

Notes:

- Direct draft alias `QA v0.6c 直写`: retested with the visible form mapping verified. `作文题目` is an `input`; `初稿` is the first `textarea`. With title `一次整理书桌` and synthetic draft body placed in the correct fields, submission produced `作文点评` and `修改小任务`. No console error was visible.
- Earlier direct draft failure was invalid for QA because the title/body fields were filled incorrectly during automation.
- Old topic alias `QA v0.6c 自画像`: topic `我的“自画像”`; selected `写一个人`; entered Step 1.
- Refresh check on alias `QA v0.6c`: Step 1 for `给老师写信` survived page reload.

## 6. Admin / 数据抽查，可选但推荐

If Admin or DB inspection is available:

- [ ] `writing_topic_idea_generation` has an LLM call log
- [ ] After AI topic ideas are generated but before selection, no Essay is created
- [ ] Essay is created after selecting a topic
- [ ] Repeated clicks on the same selection button do not create multiple Essays
- [ ] AI topic essay outline has:

  ```text
  topic_origin = ai_topic_idea
  ```

- [ ] New templates still use:

  ```text
  schema_version = v0.6b.1
  ```

Notes:

- Not checked in this manual Chrome pass; no Admin or Railway DB inspection was performed.

## Go / No-Go

### Go Conditions

- [x] New 4 topic families each have at least 1 case that can enter Step 1
- [x] AI topic generation creates 3 topics and can enter prewriting after selection
- [x] Parent summary correctly shows AI topic origin and "没有生成作文正文"
- [x] Existing direct draft path still works
- [x] No P0/P1 issues: 500, blank page, login failure, essay creation failure, or cross-family data leakage

### No-Go Conditions

- [ ] New migration causes Railway API startup failure
- [ ] AI topic selection cannot enter prewriting
- [ ] AI topic generation produces complete essay body
- [ ] AI topic failure displays hidden fallback topics
- [ ] Main school-assigned essay path is unusable
- [ ] Parent summary exposes wrong origin or shows AI ghostwritten body

## Final Decision

- Decision: Go for V0.6c targeted deployed manual QA.
- Blocking issues:
  - None found in this targeted pass.
- Follow-up issues:
  - Railway deployed commit should be cross-checked in Railway UI.
  - Optional Admin/DB inspection was not performed in this pass.
