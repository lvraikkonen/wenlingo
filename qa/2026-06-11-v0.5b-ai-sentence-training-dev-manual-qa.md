# V0.5b AI Sentence Training Dev Manual QA

Date: 2026-06-11
Environment: Dev web `https://wenlingo-web-production.up.railway.app/`
Tester: Codex QA
Result: Pass with skipped items and one non-blocking console issue

## QA Run Result - 2026-06-11

Run window: 2026-06-11 16:39-16:55 Asia/Shanghai.

Test data used:

- Family A alias: `qa***@example.com`, child `小星V05B`, grade `四年级`.
- Family B alias: `qa***@example.com`, child `小月V05B`, grade `四年级`.
- Raw invite codes and Admin token were used only during the QA session and are
  not recorded in this file.

Summary:

- Deployment smoke passed. Web home, `/alpha/start`, and API `/health` were OK.
- Admin token gate passed. `Alpha 总览`, `邀请管理`, `账号管理`, and `AI 使用量`
  rendered.
- Family A completed Magic Code login with Dev Echo `123456`, child creation,
  generated sentence challenge load, two generated challenge completions,
  free-input sentence completion, reactions, XP settlement, and parent summary.
- Admin `AI 使用量` showed aggregate rows for `sentence_challenge_generation`,
  `sentence_challenge_feedback`, and `sentence_upgrade_feedback` on 2026-06-11.
- Family B was created through an isolated direct API session and could see only
  its own child.
- Cross-family access checks rejected Family B access to Family A summary and
  Family A sentence challenge API with `404`.
- Revoked invite toggle passed using a newly generated temporary unused invite:
  hidden by default after refresh, visible when `显示已撤销邀请码` is enabled, and
  rejected by Alpha entry validation with `400`.
- Regression checks passed for assessment creation, essay draft feedback, essay
  revision comparison, reading placeholder page, parent report fallback route,
  wrong Admin token rejection, and no-cookie parent children rejection.
- Resend real email gate was later re-tested with an existing account alias
  `cl***@hotmail.com`: Magic Code request returned `200`, the mailbox received a
  real code, verify returned `200`, `/api/auth/session` returned authenticated,
  and Admin account active sessions changed from 0 to 1.
- A separate browser signup using a new unused Alpha invite and mailbox alias
  `lv***@qq.com` completed the real-email Magic Code flow and landed on
  `/parent/children` for a newly created Alpha family with no child profiles yet.

Skipped or partial:

- Daily limit was not triggered. Family A reached 12 sentence challenge
  generation calls in Dev, but the exact rest message did not appear and Admin
  `Limits` stayed `0`; this suggests the current Dev limit config is disabled or
  higher than the document's fast-path assumptions.
- Dev Echo staging/production fail-fast guard was not verified from the Dev
  browser because no staging/production-like config was available in this run.
- Browser storage enumeration was inconclusive in the in-app browser runtime
  because `localStorage` / `sessionStorage` were not exposed to the read-only
  evaluation context; no raw invite code or Magic Code was visible in page DOM or
  Admin product-event payloads.

Observed issue:

- Repeated console errors occurred on hydrated pages:
  `Minified React error #418` from a Next static chunk. Functional flows still
  worked, and no repeated CORS/network configuration errors were observed.

## Scope

Validate the V0.5b AI infrastructure and sentence challenge vertical slice after
the latest `main` branch is pushed to GitHub and Railway Dev finishes deploying.

This checklist covers:

- Resend / Dev Echo Magic Code readiness.
- AI-generated or fallback sentence challenge creation.
- Sentence challenge completion, short feedback, XP settlement, and reaction.
- `自己带句子来练` legacy free-input compatibility.
- Parent summary sentence progress.
- Admin Alpha AI usage aggregate, revoked invite toggle, and privacy boundary.
- Daily limit handling and cross-family access rejection.

Do not record raw invite codes, Admin token, Magic Codes other than the documented
Dev Echo code, full emails, full phone numbers, child writing text, raw prompts,
raw LLM responses, or AI feedback bodies in this file.

## Preconditions

- [x] Latest `main` is pushed to GitHub.
- [x] Railway Dev web deploy succeeded for `wenlingo-web`.
- [x] Railway Dev API deploy succeeded for `wenlingo-api`.
- [x] Backend migrations ran successfully, including V0.5b sentence/LLM columns.
- [x] Dev web opens: `https://wenlingo-web-production.up.railway.app/`.
- [x] API health opens from the matching Dev API host at `/health`.
- [x] `NEXT_PUBLIC_API_BASE_URL` points to the Dev API origin with no trailing slash.
- [x] `CORS_ALLOW_ORIGINS` includes `https://wenlingo-web-production.up.railway.app`
      with no trailing slash.
- [x] `ENVIRONMENT=development` on Railway Dev API.
- [x] Internal QA path is available:
  - `MAGIC_CODE_DEV_ECHO=true`
  - Magic Code is `123456`
- [x] If testing real email delivery:
  - `MAGIC_CODE_EMAIL_PROVIDER=resend`
  - `MAGIC_CODE_FROM_EMAIL` uses a verified sending domain.
  - `RESEND_API_KEY` is configured as a server-side secret.
  - `RESEND_TIMEOUT_SECONDS=10`
  - At least one real Magic Code email can be delivered from
    `MAGIC_CODE_FROM_EMAIL`.
- [x] LLM provider settings are configured on the API:
  - `LLM_PROVIDER`
  - `LLM_API_KEY`
  - `LLM_MODEL`
  - `LLM_BASE_URL`
- [x] Sentence limits are known for this QA run:
  - `SENTENCE_CHALLENGE_DAILY_LIMIT_PER_STUDENT`
  - `SENTENCE_FEEDBACK_DAILY_LIMIT_PER_STUDENT`
  - `LLM_DAILY_LIMIT_TIMEZONE`
- [x] Admin token is available to the tester but not recorded here.

## Test Data Plan

Use at least two separate Alpha families:

- Family A: primary V0.5b sentence challenge QA.
- Family B: cross-family access rejection QA.

Suggested test email format:

```text
qa-v05b-202606111523-a@example.com
qa-v05b-202606111523-b@example.com
```

Suggested child profiles:

- Family A child: `小星V05B`, `四年级`
- Family B child: `小月V05B`, `四年级`

Record only masked emails or aliases in notes.

## Step-by-Step QA

### 1. Deployment Smoke

1. Open `https://wenlingo-web-production.up.railway.app/`.
2. Open `/alpha/start` on the same Dev web origin.
3. Open the Dev API `/health` endpoint.

Expected:

- [x] Web home loads without a browser error page.
- [x] `/alpha/start` loads the Alpha entry flow.
- [x] API `/health` returns an OK status.
- [x] Browser console has no repeated CORS or network configuration errors.

Notes:

```text

```

### 2. Admin Login And Invite Generation

1. Open `https://wenlingo-web-production.up.railway.app/admin/alpha`.
2. Enter the Admin token manually.
3. Confirm Admin sections render:
   - `Alpha 总览`
   - `邀请管理`
   - `账号管理`
   - `AI 使用量`
4. Generate one invite for Family A.
5. Generate one invite for Family B.
6. Keep raw invite codes only in a private scratchpad for this QA session.

Expected:

- [x] Admin token gate accepts the valid token.
- [x] All four Admin Lite sections render.
- [x] Invite generation creates one-time raw codes.
- [x] Raw invite codes are visible only immediately after generation.
- [x] No raw invite code is recorded in this QA document.

Notes:

```text

```

### 3. Dev Echo Magic Code Login For Family A

1. Open `/alpha/start`.
2. Enter Family A invite code.
3. Enter Family A test email.
4. Request Magic Code.
5. Enter Dev Echo Magic Code `123456`.
6. Complete family/child setup if prompted.
7. Land on `/parent/children`.

Expected:

- [x] Valid invite reaches the Magic Code step.
- [x] Dev Echo code `123456` creates a valid parent session.
- [x] New family or existing family lands on `/parent/children`.
- [x] Family A child appears in the children list.
- [ ] No raw Magic Code other than `123456` is exposed in browser storage.

Notes:

```text

```

### 4. Optional Resend Real Email Gate

Run this only if the Resend sending domain is already verified.

1. Set `MAGIC_CODE_DEV_ECHO=false` on Railway Dev API.
2. Keep `ENVIRONMENT=development`.
3. Confirm Resend variables are configured.
4. Redeploy or restart `wenlingo-api`.
5. Request Magic Code for a test inbox.
6. Confirm the email arrives from `MAGIC_CODE_FROM_EMAIL`.
7. Verify login with the received code.
8. Restore the intended Dev QA auth mode after the test if needed.

Expected:

- [x] A real Magic Code email arrives after Resend domain verification.
- [x] The code creates a valid parent session.
- [ ] If the domain is not verified, external Alpha invitations remain blocked.
- [x] If this is skipped, mark it as skipped with the reason below.

Skip / result notes:

```text
Supplemental run on 2026-06-11: existing account alias `cl***@hotmail.com`
was used for a real Resend email test. Magic Code request returned 200, the
mailbox received a real code, verify returned 200, `/api/auth/session` returned
authenticated, and Admin account active sessions changed from 0 to 1. The raw
Magic Code is not recorded here.

Supplemental browser signup on 2026-06-11: mailbox alias `lv***@qq.com` was
used with a new unused Alpha invite. The real Magic Code email arrived, browser
verification succeeded, and the app landed on `/parent/children` for a newly
created Alpha family. No raw invite code or Magic Code is recorded here.
```

### 5. AI Sentence Challenge Load

1. From Family A child list, open the child dashboard.
2. Open the sentence workshop.
3. Observe the initial mode.

Expected:

- [x] Sentence workshop defaults to generated challenge mode.
- [x] Page shows `正在准备今天的句子挑战` while loading.
- [x] Page then shows an AI-generated or fallback challenge.
- [x] Challenge includes source sentence, prompt, hint, focus, grade/difficulty.
- [x] It does not show a standard answer for the child to copy.
- [ ] If AI generation fails, fallback challenge still lets the child proceed.

Notes:

```text

```

### 6. Complete One Generated Challenge

1. In the generated challenge answer box, write an upgraded sentence.
2. Click `提交给 AI 教练`.
3. Wait for feedback and settlement.
4. Click one child feedback reaction, for example `有帮助`.

Expected:

- [x] Submit button disables or shows pending state while submitting.
- [x] Short feedback appears under `AI 教练反馈`.
- [x] Feedback includes encouragement, highlight, suggestion, and a short example.
- [x] XP settlement appears.
- [x] Feedback reaction saves and keeps the selected pressed state.
- [x] `回到 Dashboard` link points back to the child dashboard.
- [x] `给家长看报告` link is present.

Notes:

```text

```

### 7. Challenge Again

1. Click `再挑战一题`.
2. Confirm a new challenge loads.
3. Complete the second challenge.

Expected:

- [x] Previous answer and feedback are cleared.
- [x] A new generated or fallback `SentenceTraining` challenge row is created.
- [x] Completing the second challenge shows short feedback and XP again.
- [x] Admin AI usage later reflects additional generation/feedback calls.

Notes:

```text

```

### 8. Legacy Free-Input Sentence Flow

1. In the sentence workshop, click `自己带句子来练`.
2. Enter an original sentence in `原句`.
3. Enter an upgraded sentence in `升级后的句子`.
4. Click `提交给 AI 教练`.
5. Save one reaction.

Expected:

- [x] Free-input mode shows both `原句` and `升级后的句子`.
- [x] Old sentence training endpoint still works.
- [x] Feedback includes encouragement, specific improvement, next step, and
      problem monster labels.
- [x] XP settlement appears.
- [x] Reaction saves for the free-input result.

Notes:

```text

```

### 9. Parent Summary Sentence Progress

1. Open the parent summary for Family A child.
2. Review the practice counts and sentence section.

Expected:

- [x] Parent summary loads without server error.
- [x] Sentence training count reflects completed sentence work.
- [x] Lightweight sentence challenge progress or summary is shown.
- [x] Parent summary does not expose the child's raw sentence text.
- [x] Parent summary feedback controls still work.

Notes:

```text

```

### 10. Admin AI Usage Aggregate

1. Open `/admin/alpha`.
2. Enter Admin token if needed.
3. Scroll to `AI 使用量`.
4. Review usage rows after completing sentence challenges.

Expected:

- [x] `AI 使用量` section renders.
- [x] Rows appear for sentence challenge generation and/or feedback after use.
- [x] Rows show date, task, model, calls, tokens, cost, failures, and limits.
- [x] Usage is aggregate only; it does not expose child writing text, prompts, or
      raw LLM responses.

Notes:

```text

```

### 11. Daily Limit Path

Primary path:

1. Continue generating and completing challenges for the same Family A child
   until the configured daily limit is reached.
2. Try to load another challenge.

Fast QA path, only if Dev config can be temporarily adjusted:

1. Set `SENTENCE_CHALLENGE_DAILY_LIMIT_PER_STUDENT=1` and/or
   `SENTENCE_FEEDBACK_DAILY_LIMIT_PER_STUDENT=1` on Dev API.
2. Redeploy or restart `wenlingo-api`.
3. Complete one challenge.
4. Try to load or complete another challenge for the same child.
5. Restore the intended limit values after QA.

Expected:

- [ ] Daily limit response appears in the child UI.
- [ ] Exact rest message is:

```text
今天的句子挑战已经完成很多啦，休息一下，明天继续闯关！
```

- [ ] Admin `AI 使用量` shows daily limit hit count after the limit path.
- [ ] The app remains usable after the limit message.

Notes:

```text

```

### 12. Revoked Invite Toggle

1. In Admin, generate a new unused invite.
2. Revoke that unused invite.
3. Confirm it disappears from the default invite list.
4. Turn on `显示已撤销邀请码`.
5. Confirm the revoked invite appears.
6. Try the revoked invite at `/alpha/start`.

Expected:

- [x] Revoked invites are hidden by default.
- [x] The toggle loads revoked invites.
- [x] Revoked invite is rejected at Alpha entry.
- [x] Consumed invites cannot be revoked.

Notes:

```text

```

### 13. Family B Setup For Cross-Family Checks

1. In a separate browser profile, incognito window, or after logging out, open
   `/alpha/start`.
2. Use Family B invite.
3. Request Magic Code.
4. Enter `123456` if Dev Echo is active.
5. Create or confirm Family B child.
6. Record Family B child id only in a private scratchpad.

Expected:

- [x] Family B has a separate parent session.
- [ ] Family B can open its own child dashboard and sentence workshop.
- [x] Family B cannot see Family A child in `/parent/children`.

Notes:

```text

```

### 14. Cross-Family Sentence Challenge Access Rejection

1. While logged in as Family B, try to open Family A child sentence route:
   `/children/<family-a-child-id>/sentence`.
2. Try to open Family A child summary route:
   `/parent/children/<family-a-child-id>/summary`.
3. If using API tools, try completing a Family A challenge id with Family B's
   session cookie.

Expected:

- [x] Cross-family child route is rejected, redirected, or shows a safe not-found
      state.
- [x] Cross-family parent summary is rejected, redirected, or shows a safe
      not-found state.
- [ ] API completion for another family's challenge is rejected.
- [x] Rejection does not reveal Family A child writing text or challenge answer.

Notes:

```text

```

### 15. Dev Echo Guard Boundary

This check is usually performed by configuration review or a temporary
staging/production-like deploy, not by the Dev browser alone.

1. Confirm Dev uses `ENVIRONMENT=development` if `MAGIC_CODE_DEV_ECHO=true`.
2. Confirm staging/production configs do not set `MAGIC_CODE_DEV_ECHO=true`.
3. If a production-like config is available, confirm startup fails fast when
   `ENVIRONMENT=staging` or `ENVIRONMENT=production` and
   `MAGIC_CODE_DEV_ECHO=true`.

Expected:

- [x] Dev Echo is allowed only for internal QA in `ENVIRONMENT=development`.
- [ ] Staging/production do not boot with Dev Echo enabled.
- [ ] The guard does not rely on database URL heuristics.

Notes:

```text

```

### 16. Privacy And Logging Boundary

1. In Admin family detail/timeline, inspect recent product events.
2. Inspect `AI 使用量`.
3. Review browser storage for the QA session.

Expected:

- [x] ProductEvent payloads do not include raw child writing, raw upgraded
      sentences, raw prompts, raw LLM responses, full invite codes, full emails,
      phone numbers, addresses, schools, or photos.
- [x] Admin AI usage exposes only aggregate counts and costs.
- [ ] Browser storage does not contain raw invite codes or Magic Codes.

Notes:

```text

```

## Regression Checklist

- [x] Demo login still works.
- [x] Assessment flow still creates the first ability sketch.
- [x] Essay draft feedback still works.
- [x] Essay revision comparison still works.
- [x] Reading page remains a controlled placeholder.
- [x] Parent report route shows content or controlled fallback, not a server error.
- [x] Wrong Admin token returns an access error.
- [x] No-cookie parent children access is rejected.

## Pass / Fail Summary

Overall status:

- [ ] Pass
- [x] Pass with skipped items
- [ ] Fail

Skipped items and reason:

```text
- Daily limit path partial/skipped: current Dev config did not surface the exact
  rest message after 12 generated challenge calls for Family A; Admin aggregate
  `Limits` remained 0. Follow-up investigation confirmed the Dev API was missing
  `LLM_DAILY_LIMIT_ENABLED=true`, so this was a configuration gap rather than a
  verified code failure.
- Dev Echo staging/production guard skipped: no staging/production-like config
  was available to verify startup fail-fast behavior.
- Browser storage enumeration inconclusive: in-app browser read-only evaluation
  did not expose localStorage/sessionStorage, but raw invite codes/Magic Codes
  were not visible in DOM or Admin product-event payloads.
```

## V0.5b Hotfix Follow-Up Validation

These checks were added after the 2026-06-11 Dev QA run and should be re-run
after the hotfix deploy.

### Existing Account Login Without Invite

1. In a fresh browser/profile with no `wenlingo_parent_session`, open
   `/alpha/start`.
2. Choose `已有账号登录`.
3. Enter an already-linked Alpha account email.
4. Request and verify a real Magic Code.

Expected:

- [ ] The login path does not require an invite code.
- [ ] The Magic Code request is sent for the email.
- [ ] Verification lands on `/parent/children` for the linked family.
- [ ] An authenticated but unlinked email shows a clear no-family message and
      does not create a new family.

### Manual Alpha Account Cleanup

1. Run the cleanup ops script with `--dry-run` for a known QA account.
2. Run the cleanup ops script for a real-domain test inbox only with
   `--allow-real-email` and the required confirmation text.
3. Attempt the same command without `--allow-real-email`.
4. Confirm the script refuses to run when `ENVIRONMENT=production`.

Expected:

- [ ] Dry-run reports masked email and object counts only.
- [ ] Real-domain test inbox deletion is blocked unless `--allow-real-email` is
      present.
- [ ] Confirmed deletion removes the account, family, children, sessions,
      consumed invites, Magic Codes, and dependent learning rows.
- [ ] Script output and logs do not expose full emails or child writing text.

### Environment Variable Docs And Daily Limit

1. Confirm `docs/env-vars.md` and `README.md` document the Dev/Railway required
   variables.
2. Set `LLM_DAILY_LIMIT_ENABLED=true` on Dev API with the intended sentence
   challenge/feedback limits.
3. Redeploy/restart API and repeat the Daily limit path.

Expected:

- [ ] README points to the full env var checklist.
- [ ] The checklist covers API, Web, Auth/session, Magic Code, Resend/SMTP, LLM,
      limits/cost, and redeploy requirements.
- [ ] Daily limit QA shows the expected rest message after the configured limit.
- [ ] Admin `AI 使用量` shows a limit hit after the limit path.

Failures:

| ID | Step | Expected | Actual | Screenshot / logs | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- |
| QA-2026-06-11-01 | Step 1 / general browser console | No repeated browser console errors during main flows | Repeated `Minified React error #418` hydration errors appeared on several page loads; no repeated CORS/network config errors and functional flows still passed | Browser console via in-app browser | Web | Open |

Release decision:

```text
Continue Dev QA / internal Alpha validation. Functional V0.5b sentence training
slice passed, with skipped environment-gated checks and one non-blocking React
hydration console issue to fix before a release-candidate signoff.
```
