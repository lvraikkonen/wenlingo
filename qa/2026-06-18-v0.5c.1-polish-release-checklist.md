# V0.5c.1 Polish Release Checklist

Date: 2026-06-18
Tester: Codex + Chrome extension manual QA
Environment: Railway Dev
Web URL: https://wenlingo-web-production.up.railway.app
API URL: not recorded; external Alpha should use Web origin `/api/*`
Commit: local HEAD `dcf99b5`; Railway deployed commit identity user-verified

Do not record raw Admin tokens, Magic Codes, full emails, full phone numbers,
raw invite codes, child writing text, raw prompts, raw LLM responses, or full AI
feedback bodies in this file.

Run notes:

- Chrome manual QA run from Web origin on 2026-06-18 PT.
- User confirmed Railway env updates:
  - `wenlingo-api`: removed `LLM_PROMPT_VERSION`.
  - `wenlingo-web`: `API_PROXY_TARGET=https://<wenlingo-api-origin>`.
  - `wenlingo-web`: `NEXT_PUBLIC_API_BASE_URL=` empty.
- Chrome direct navigation to `/api/health` was blocked by the browser with
  `ERR_BLOCKED_BY_CLIENT`; same-origin API behavior was instead checked through
  the Alpha UI.
- Magic Code request for a synthetic QA email reached the code-entry state.
  Wrong code showed the expected code-specific error. The configured Dev Echo
  test code was rejected, so real email-code login was not completed in this
  run.
- `/parent/children` without a usable parent session eventually redirected back
  to `/alpha/start`.
- `/admin/alpha` showed the Admin token gate; no admin token was entered.
- Follow-up Chrome run used a user-provided existing account email and
  user-provided Admin token. Raw values are intentionally not recorded here.
- Existing-account Magic Code request reached the code-entry state without an
  on-page send error. Successful login is pending receipt of the emailed code.
- Admin token was accepted. Admin read-only checks verified masked account
  emails, AI usage aggregate columns/data, and one family timeline payload
  without raw writing/prompt/response/email/phone/invite-code keys.
- User-provided Magic Code was accepted. Browser landed on `/parent/children`,
  refresh remained authenticated, and an existing child profile opened from the
  family page.
- One sentence challenge generated successfully, one QA-written sentence
  submission returned AI feedback and battle settlement, and parent-visible
  report/summary pages opened through in-app navigation.
- User confirmed Railway API env values:
  `AUTH_SESSION_COOKIE_SECURE=true`, `AUTH_SESSION_COOKIE_SAMESITE=lax`,
  `AUTH_ALLOWED_ORIGINS=https://wenlingo-web-production.up.railway.app`,
  `CORS_ALLOW_ORIGINS=https://wenlingo-web-production.up.railway.app`,
  `LLM_DAILY_LIMIT_PER_STUDENT_TASK=6`,
  `SENTENCE_CHALLENGE_DAILY_LIMIT_PER_STUDENT=3`, and
  `SENTENCE_FEEDBACK_DAILY_LIMIT_PER_STUDENT=3`.
- User confirmed Railway Web env values:
  `API_PROXY_TARGET=https://wenlingo-api-production.up.railway.app` and
  `NEXT_PUBLIC_API_BASE_URL=` empty.
- Chrome Incognito existing-account Magic Code request reached the code-entry
  state without an on-page send error. User-provided Magic Code was accepted,
  browser landed on `/parent/children`, and refresh remained authenticated.
- User confirmed mobile Safari login succeeded with the same existing account.
- Daily-limit QA used the current Chrome Incognito logged-in account after the
  narrowed limits were effective. On 2026-06-19, Admin AI usage baseline had
  `sentence_challenge_generation=1` provider success and
  `sentence_challenge_feedback=1` provider success. After two more sentence
  challenge completions and one over-limit generation attempt, Admin showed
  generation provider successes at `3`, feedback provider successes at `3`, and
  one `sentence_challenge_generation` `daily_limit_reached` local-fallback row
  with `0` tokens and `0ms` latency. The child-facing UI showed the rest
  message.
- User manually verified the Railway deployed commit identity and restoration
  of normal Alpha daily-limit values after the temporary limit QA.

## Environment

- [x] Web and API deploys point at the intended commit. User manually verified
      the Railway deployed commit identity; local HEAD was `dcf99b5`.
- [x] `API_PROXY_TARGET` is set for external Alpha. User confirmed.
- [x] Browser API calls use Web origin `/api/*`. Alpha UI completed Magic Code
      request/verify attempts from the Web origin with `NEXT_PUBLIC_API_BASE_URL`
      empty per user-confirmed env.
- [x] `AUTH_SESSION_COOKIE_SECURE=true`. User confirmed Railway API env.
- [x] `AUTH_SESSION_COOKIE_SAMESITE=lax` for same-origin Alpha. User confirmed
      Railway API env.
- [x] `MAGIC_CODE_DEV_ECHO` setting matches test intent. Observed Dev Echo
      test-code rejection; this is consistent with real-email QA rather than
      Dev Echo QA.
- [x] Daily-limit env values match test intent. User confirmed API env values:
      general per-student task limit `6`, sentence challenge limit `3`, and
      sentence feedback limit `3`.

## Same-Origin Proxy

- [x] Railway Web has `API_PROXY_TARGET=https://<wenlingo-api-service-origin>`.
      User confirmed.
- [x] Railway Web has `NEXT_PUBLIC_API_BASE_URL=` or `NEXT_PUBLIC_API_BASE_URL=/api`.
      User confirmed empty value.
- [x] Browser network requests for Alpha pages call the Web origin `/api/*`, not the API service origin.
      Alpha and Admin UI requests succeeded from the Web origin with
      `NEXT_PUBLIC_API_BASE_URL` empty; Chrome network export was not captured.
- [x] Railway API has `CORS_ALLOW_ORIGINS=https://<wenlingo-web-origin>`.
      User confirmed Railway API env.
- [x] Railway API has `AUTH_ALLOWED_ORIGINS=https://<wenlingo-web-origin>`.
      User confirmed Railway API env.
- [x] Login cookie is `Secure`, `SameSite=Lax`, `Path=/`, and has no cookie domain.
      User manually verified the cookie attributes in Chrome DevTools.
- [x] Direct cross-site API URL is not shared with external Alpha families.
      Checklist records only the Web URL.

## Login

- [x] Chrome normal Magic Code login. Request-code state verified and
      user-provided Magic Code landed on `/parent/children`.
- [x] Chrome Incognito Magic Code login. Existing-account login landed on
      `/parent/children` and refresh remained authenticated.
- [x] Safari or mobile browser Magic Code login. User confirmed successful
      mobile Safari login.
- [x] Refresh `/parent/children` after login and remain authenticated.
- [x] Invalid Magic Code shows code-specific error:
      `验证码不正确或已过期，请重新输入。`
- [x] Missing session returns user to `/alpha/start` from `/parent/children`.

## Core Learning Flow

- [x] Parent children page opens.
- [x] Child Dashboard opens.
- [x] Sentence challenge generation works.
- [x] Sentence feedback works.
- [x] Parent summary/report opens.
- [x] Core pages provide in-app navigation without browser Back/Forward.

## Navigation

- [x] `/alpha/start` provides an in-app route to the next expected step after login.
      Existing-account login redirected to `/parent/children`.
- [x] `/parent/children` provides in-app navigation to each child dashboard.
- [x] Child Dashboard provides in-app navigation to sentence practice and parent-visible progress.
- [x] Sentence Workshop provides in-app navigation back to dashboard and parent summary/report.
- [x] Parent summary/report provides in-app navigation back to child or parent family views.

## AI Reliability

- [x] Primary success is logged. Admin AI usage showed `primary_success`.
- [x] Fallback success is logged. Admin AI usage showed `fallback_success`.
- [x] Daily-limit hit returns child-friendly rest message. Chrome Incognito UI
      showed `今天的句子挑战已经完成很多啦，休息一下，明天继续闯关！`.
- [x] Daily-limit hit creates no provider call. Admin AI usage showed provider
      calls stopped at the configured sentence generation limit, followed by a
      `daily_limit_reached` local-fallback row with `0` tokens and `0ms`
      latency.
- [x] Admin AI usage shows limit hits and fallback status. Admin AI usage table
      showed statuses including `daily_limit_reached`, fallback columns, token
      totals, costs, latency, and nonzero limit-hit aggregates.

## Daily-Limit Atomicity

- [x] Temporarily lower one sentence daily limit for QA and record the variable/value here:
      User confirmed `SENTENCE_CHALLENGE_DAILY_LIMIT_PER_STUDENT=3` and
      `SENTENCE_FEEDBACK_DAILY_LIMIT_PER_STUDENT=3`.
- [x] Redeploy/restart API after temporary limit change. Not independently
      observed in Railway, but Chrome/Admin behavior verified the changed
      values were effective.
- [x] Trigger concurrent or rapid repeated attempts at the limit boundary.
      Performed rapid repeated UI attempts in Chrome Incognito. User accepted
      deferring the true concurrent race test to later performance testing.
- [x] Exactly the allowed number of attempts creates provider calls. Admin AI
      usage for 2026-06-19 showed generation provider successes at `3` and
      feedback provider successes at `3`.
- [x] Over-limit attempts return the child-friendly rest message.
- [x] Over-limit attempts create no provider calls. The over-limit generation
      row was `daily_limit_reached` / `local_fallback` with `0` tokens and
      `0ms` latency.
- [x] Admin AI usage shows daily-limit hits.
- [x] Restore normal Alpha daily limit values and record restore time here:
      User manually verified restoration after temporary limit QA. Exact
      restore time was not recorded in this checklist.

## Privacy

- [x] Admin pages do not show raw child writing. Read-only Admin overview,
      account management, AI usage, and one family timeline were checked.
- [x] Admin pages do not show raw prompts or raw LLM responses. AI usage showed
      aggregate metadata only; no raw prompt/response labels or bodies observed.
- [x] Admin pages do not show raw Magic Codes, invite codes, API keys, full emails, or full phone numbers.
      Existing account emails were masked, phone display was bound/unbound only,
      and no generated raw invite-code panel was visible.

## Admin Privacy

- [x] Admin Alpha overview masks account identifiers. Account emails appeared in
      masked form. Family display names remain visible by design.
- [x] Admin family timeline shows event metadata without child writing text.
      One selected timeline payload showed event metadata only.
- [x] Admin AI usage shows aggregate usage, final status, fallback status, costs, tokens, latency, and limit hits without raw prompt or response bodies.
      Verified through AI usage table headers and aggregate rows.
- [x] Admin account management does not expose raw Magic Codes, invite codes, API keys, full emails, or full phone numbers.
      Verified masked emails and no raw codes/secrets in the visible account
      management area.

## Scope Guard

- [x] QA did not require V0.6 features.
- [x] QA did not add or rely on new AI tasks.
- [x] QA did not require advanced Admin reporting beyond V0.5c.1 polish scope.

## Go / No-Go

- [x] Go
- [ ] No-Go

Notes:

- Go for V0.5c.1 polish release based on completed manual QA. The true
  concurrent daily-limit race test is explicitly deferred to later performance
  testing.
- Raw Magic Code, raw account email, raw Admin token, raw QA sentence, raw AI
  feedback body, and raw child writing are intentionally omitted from this file.
