# V0.5a Alpha User Foundation Manual QA

Date:
Tester:
Environment:
API commit:
Web commit:

## Pre-Rollout

- [ ] Production database backup confirmed.
- [ ] `AUTH_REQUIRED_FOR_ALPHA=false` deployed and legacy flow still works.
- [ ] `MAGIC_CODE_DEV_ECHO=false` in production.
- [ ] Production Magic Code email provider configured after the real sender/provider integration ships; current build has no real production email provider, so production auth remains blocked.
- [ ] Production auth remains blocked until the real Magic Code email sender/provider integration is smoke-tested with `MAGIC_CODE_DEV_ECHO=false`.
- [ ] Current build behavior audited: with `MAGIC_CODE_DEV_ECHO=false`, Magic Code requests return 503 because `get_email_sender()` uses `DisabledProductionEmailSender`.
- [ ] Deployment gate/manual config audit confirms `MAGIC_CODE_DEV_ECHO=false` before any production auth enablement; current app does not fail startup automatically if dev echo is true unless a future startup guard is added.

## New Family

- [ ] Create invite code.
- [ ] Request Magic Code for new email.
- [ ] Same response appears for existing and new email.
- [ ] Wrong code shows friendly generic error.
- [ ] Expired code shows friendly generic error.
- [ ] Resend invalidates older code.
- [ ] Same email rate limit triggers after 3 requests in 10 minutes.
- [ ] New family creates parent and consumes invite.
- [ ] Create child.
- [ ] Complete assessment.
- [ ] Complete sentence training.
- [ ] Complete essay draft and revision.
- [ ] Save child reaction.
- [ ] Open parent summary.
- [ ] Save parent summary feedback.

## Existing Family Migration

- [ ] Browser with `wenlingo_alpha_parent_id` and no cookie sees migration flow.
- [ ] Email Magic Code binds legacy parent.
- [ ] Existing child list remains unchanged.
- [ ] Existing assessment/sentence/essay counts remain unchanged.
- [ ] Existing reaction and parent summary feedback state remains unchanged.
- [ ] Conflict page says `这个家庭已经绑定过账号，请联系邀请人处理。`
- [ ] Conflict page does not reveal another account email.

## Security Boundaries

- [ ] No cookie returns 401 for parent endpoint.
- [ ] Cross-family student id returns 404.
- [ ] Cross-family essay revision returns 404.
- [ ] Wrong admin token returns 403.
- [ ] Non-JSON authenticated state-changing requests are rejected for JSON-body endpoints: `POST /api/alpha/parents`, `POST /api/alpha/legacy-parent-bind`, `POST /api/alpha/parents/me/children`, `POST /api/alpha/parents/{parent_id}/children`, `POST /api/students/{student_id}/feedback-reactions`, `POST /api/alpha/parents/me/children/{student_id}/summary-feedback`, `POST /api/alpha/parents/{parent_id}/children/{student_id}/summary-feedback`, `POST /api/students/{student_id}/essays`, `POST /api/essays/{essay_id}/revision`, and `PATCH /api/auth/account/phone`.
- [ ] Invalid Origin/Referer is rejected in staging for authenticated mutating endpoints: `POST /api/auth/logout`, `PATCH /api/auth/account/phone`, `POST /api/alpha/parents`, `POST /api/alpha/legacy-parent-bind`, `POST /api/alpha/parents/me/children`, `POST /api/alpha/parents/{parent_id}/children`, `POST /api/students/{student_id}/feedback-reactions`, `POST /api/alpha/parents/me/children/{student_id}/summary-feedback`, `POST /api/alpha/parents/{parent_id}/children/{student_id}/summary-feedback`, `POST /api/students/{student_id}/essays`, and `POST /api/essays/{essay_id}/revision`.
- [ ] `POST /api/auth/logout` is explicitly verified as a security blocker until it rejects invalid Origin/Referer.
- [ ] `PATCH /api/auth/account/phone` is explicitly verified as a security blocker until it rejects invalid Origin/Referer and non-JSON bodies.
- [ ] Admin does not show child writing text or AI feedback body.

## Rollback

- [ ] Set `AUTH_REQUIRED_FOR_ALPHA=false`.
- [ ] Legacy localStorage parent flow works.
- [ ] Invite, assessment, sentence, essay, feedback, summary, and admin flow still work.
- [ ] No learning rows deleted.
