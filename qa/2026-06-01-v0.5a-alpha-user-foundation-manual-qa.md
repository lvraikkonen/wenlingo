# V0.5a Alpha User Foundation Manual QA

Date: 2026-06-04
Tester: Codex manual QA
Environment: Dev web https://wenlingo-web-production.up.railway.app/alpha/start; API https://wenlingo-api-production.up.railway.app
API commit: Not verified from deployment metadata.
Web commit: Not verified from deployment metadata.

## 2026-06-04 Manual QA Notes

- Invite used: `ALPHA-V8IZJIIKBTIH` (provided by user).
- Test email used for initial Magic Code request/rate-limit check: `qa-1780552512559@example.com`.
- The valid invite moved the UI into the 6-digit Magic Code step, but the code is not displayed by the frontend. End-to-end new family creation is blocked until a received 6-digit code or a reachable email inbox is provided.
- Direct API checks used the production API base discovered from the deployed web bundle: `https://wenlingo-api-production.up.railway.app`.
- Browser initially showed the legacy migration entry because a `wenlingo_alpha_parent_id` localStorage value was already present. Clicking "重新创建 Alpha 家庭" cleared that local identity and showed the new-family invite form.
- A valid Admin Alpha token was provided after the initial pass. Admin API checks returned 6 overview rows and 3 consumed-family detail payloads. Detail payload keys were limited to summary/event metadata such as `children`, `grade_label`, `event_type`, `payload`, `reaction_counts`, and `parent_feedback`; no forbidden writing/AI feedback body keys were present.
- Admin frontend token entry could not be fully re-tested through the in-app browser because automated password input was blocked by the browser virtual clipboard/input runtime. API output was verified directly.
- Full browser regression continued with user-assisted text entry. New account/family `qa-full-test-father` was created with child `小星QA` (`student_id=766d27f3-d350-455c-ab8f-e5bda2b50a90`) and reached `/parent/children`.
- Learning flow results: assessment completed, sentence training completed, essay draft feedback completed, essay revision feedback completed, child feedback reactions submitted, parent summary opened, and parent summary feedback submitted.
- Reading/report result: `/children/766d27f3-d350-455c-ab8f-e5bda2b50a90/reading` loads but shows `阅读峡谷施工中`; `/parent/766d27f3-d350-455c-ab8f-e5bda2b50a90/report` shows a server error (`This page couldn’t load`, `ERROR 1491425775`).
- LocalStorage/cookie result: using the UI to clear stored Alpha parent id and reloading `/alpha/start` did not redirect back to the family; however direct `/parent/children` still loaded the family via cookie session, so family access was retained.
- Cross-family browser result: current session visiting another family's summary/dashboard showed failure alerts (`成长摘要加载失败，请稍后再试。`, `孩子空间加载失败，请稍后再试。`) and did not reveal the other child data.
- Admin frontend with correct token loaded overview rows and current family detail; detail showed masked account state, event timeline, sanitized payload metadata, and no writing/AI feedback body.

## Pre-Rollout

- [ ] Production database backup confirmed.
- [ ] `AUTH_REQUIRED_FOR_ALPHA=false` deployed and legacy flow still works.
- [ ] `MAGIC_CODE_DEV_ECHO=false` in production.
- [ ] Production Magic Code email provider configured after the real sender/provider integration ships; current build has no real production email provider, so production auth remains blocked.
- [ ] Production auth remains blocked until the real Magic Code email sender/provider integration is smoke-tested with `MAGIC_CODE_DEV_ECHO=false`.
- [ ] Current build behavior audited: with `MAGIC_CODE_DEV_ECHO=false`, Magic Code requests return 503 because `get_email_sender()` uses `DisabledProductionEmailSender`.
- [ ] Deployment gate/manual config audit confirms `MAGIC_CODE_DEV_ECHO=false` before any production auth enablement; current app does not fail startup automatically if dev echo is true unless a future startup guard is added.

## New Family

- [ ] Create invite code. Not performed by tester; invite `ALPHA-V8IZJIIKBTIH` was provided externally.
- [x] Request Magic Code for new email. Valid invite + `qa-1780552512559@example.com` moved UI to the "6 位验证码" step with no console errors.
- [ ] Same response appears for existing and new email. Not verified; no known existing account email provided.
- [x] Wrong code shows friendly generic error. Entering `000000` showed `登录或创建失败，请检查后再试。`.
- [ ] Expired code shows friendly generic error. Not verified; no expired Magic Code available.
- [ ] Resend invalidates older code. Not verified; frontend does not expose generated codes and no inbox/code was available.
- [x] Same email rate limit triggers after 3 requests in 10 minutes. Fourth request for the same email showed `验证码发送失败，请稍后再试。`.
- [x] New family creates parent and consumes invite. Browser flow with fixed dev Magic Code `123456` entered `/parent/children` for `qa-full-test-father`; admin later showed `Alpha Family 04 consumed` and `Account linked`.
- [x] Create child. Created child `小星QA`; handoff page displayed `小星QA已加入小文星球！`.
- [x] Complete assessment. Completed assessment; page displayed `第一张能力草图`, ability scores, and `+20 XP`.
- [x] Complete sentence training. Completed sentence training; page displayed `AI 教练反馈`, `+25 XP`, and `first_sentence_upgrade`.
- [x] Complete essay draft and revision. Completed essay draft feedback and second-draft revision; page displayed `作文点评`, `二稿对比`, `+60 XP`, and `first_revision`.
- [x] Save child reaction. Submitted positive reactions for assessment, sentence, essay draft, and essay revision; buttons changed to disabled/pressed states.
- [x] Open parent summary. Opened `/parent/children/766d27f3-d350-455c-ab8f-e5bda2b50a90/summary`; summary showed practice counts and ability changes.
- [x] Save parent summary feedback. Clicked `有帮助`; summary feedback button changed to pressed state.

## Existing Family Migration

- [x] Browser with `wenlingo_alpha_parent_id` and no cookie sees migration flow. Initial `/alpha/start` load showed `绑定邮箱继续使用当前 Alpha 家庭` with `绑定并继续` and `重新创建 Alpha 家庭`.
- [ ] Email Magic Code binds legacy parent. Not verified; no reachable email/code and no confirmed legacy parent id scenario provided.
- [ ] Existing child list remains unchanged. Not verified; requires a known legacy family before/after bind.
- [ ] Existing assessment/sentence/essay counts remain unchanged. Not verified; requires a known legacy family before/after bind.
- [ ] Existing reaction and parent summary feedback state remains unchanged. Not verified; requires a known legacy family before/after bind.
- [ ] Conflict page says `这个家庭已经绑定过账号，请联系邀请人处理。` Not verified; requires an already-bound legacy family/account conflict scenario.
- [ ] Conflict page does not reveal another account email. Not verified; requires an already-bound legacy family/account conflict scenario.

## Security Boundaries

- [x] No cookie returns 401 for parent endpoint. `GET https://wenlingo-api-production.up.railway.app/api/alpha/parents/me/children` returned `401 {"detail":"parent session required"}`.
- [x] Cross-family student id returns 404. Browser check with current family session against another family's student id did not reveal data: summary showed `成长摘要加载失败，请稍后再试。`, dashboard showed `孩子空间加载失败，请稍后再试。`.
- [ ] Cross-family essay revision returns 404.
- [x] Wrong admin token returns 403. `GET https://wenlingo-api-production.up.railway.app/api/admin/alpha/overview` with `x-alpha-admin-token: wrong-token-qa` returned `403 {"detail":"admin token required"}`.
- [ ] Expected guarded/pass checks reject invalid Origin/Referer and non-JSON authenticated state-changing requests: `POST /api/alpha/parents`, `POST /api/alpha/legacy-parent-bind`, `POST /api/students/{student_id}/assessment`, `POST /api/students/{student_id}/sentences`, `POST /api/students/{student_id}/readings`, `POST /api/students/{student_id}/reports`, `POST /api/students/{student_id}/feedback-reactions`, `POST /api/students/{student_id}/essays`, and `POST /api/essays/{essay_id}/revision`. Not verified; requires an authenticated parent session cookie and valid family/student/essay ids.
- [ ] Known blocker: `POST /api/auth/logout` rejects invalid Origin/Referer before production auth can be enabled. Not verified; requires authenticated session context.
- [ ] Known blocker: `PATCH /api/auth/account/phone` rejects invalid Origin/Referer and non-JSON bodies before production auth can be enabled. Not verified; requires authenticated session context.
- [ ] Known blocker: `POST /api/alpha/parents/me/children` rejects invalid Origin/Referer and non-JSON bodies before production auth can be enabled. Not verified; requires authenticated session context.
- [ ] Known blocker: `POST /api/alpha/parents/{parent_id}/children` rejects invalid Origin/Referer and non-JSON bodies before production auth can be enabled. Not verified; requires authenticated session context.
- [ ] Known blocker: `POST /api/alpha/parents/me/children/{student_id}/summary-feedback` rejects invalid Origin/Referer and non-JSON bodies before production auth can be enabled. Not verified; requires authenticated session context.
- [ ] Known blocker: `POST /api/alpha/parents/{parent_id}/children/{student_id}/summary-feedback` rejects invalid Origin/Referer and non-JSON bodies before production auth can be enabled. Not verified; requires authenticated session context.
- [x] Admin does not show child writing text or AI feedback body. Verified via Admin Alpha API with valid token: overview returned 6 rows; 3 consumed-family detail payloads had no forbidden writing text / sentence text / essay text / AI feedback body keys. Browser frontend with correct token loaded overview and current family detail showing masked account state, timeline event names, target ids, statuses, reaction/usefulness metadata, and no child writing text or AI feedback body.

## Rollback

- [ ] Set `AUTH_REQUIRED_FOR_ALPHA=false`.
- [ ] Legacy localStorage parent flow works.
- [ ] Invite, assessment, sentence, essay, feedback, summary, and admin flow still work.
- [ ] No learning rows deleted.
