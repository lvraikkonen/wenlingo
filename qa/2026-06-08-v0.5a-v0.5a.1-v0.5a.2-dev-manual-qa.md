# V0.5a / V0.5a.1 / V0.5a.2 Dev Manual QA

Date: 2026-06-08
Environment: Dev web `https://wenlingo-web-production.up.railway.app/`
Tester: Codex manual QA

## Scope

Validated the V0.5a, V0.5a.1, and V0.5a.2 QA checklist items against the current Railway Dev deployment, within the documented current limitation that real Magic Code email delivery remains skipped while Railway outbound SMTP reachability is unresolved.

Sensitive values are intentionally omitted: raw invite codes, Admin token, full test email, full phone number, child writing text, and AI feedback body.

## Summary

Status: not fully closed.

Core learning flows pass, and most V0.5a.2 hardening items pass. Three V0.5a.2 closure items still failed in this run:

- Disabled-account verify did not show `账号暂不可用，请联系邀请人。`; actual UI showed `绑定失败，请稍后再试。`.
- Re-enabled legacy-bind login created a usable session, but `/alpha/start` still showed `绑定失败，请稍后再试。` instead of landing on `/parent/children`.
- Admin hard delete of an allowlisted test account returned `500 Internal Server Error`; the test account remained undeleted.

Additional observation:

- Phone binding initially passed and Admin overview reported `phone_bound=true`, but after the re-enabled legacy-bind login path, `/parent/children` showed the phone as unbound while Admin overview still showed `phone_bound=true`.

## Passed

- Admin token loaded `/admin/alpha`; account list showed masked emails and active session counts.
- Admin generated a one-time invite; raw invite was visible once and was not found in browser storage.
- Admin invalid invite count checks for `0` and `21` did not create additional raw invites.
- Admin revoked an unused invite; revoked invite was rejected at `/alpha/start` with `邀请码无效或已失效，请检查后再试。`.
- Dev Echo auth created a new Alpha family from `/alpha/start` and landed on `/parent/children`.
- Consumed invite appeared as consumed and had no `Revoke invite` action.
- Phone unbound state allowed binding with a valid test phone format.
- Phone bound state hid the input and showed `修改手机号`.
- Phone edit flow exposed input plus `取消`, saved a new valid test phone, and returned to read-only bound state.
- Admin Disable account showed row-level `Disabling...`, revoked the active session, changed active sessions from 1 to 0, and changed the action to `Enable account`.
- Disabled account Magic Code request stayed generic and reached the verification step.
- Admin Enable account showed row-level `Enabling...` and returned the action to `Disable account`.
- Admin hard delete mixed batch containing a non-test account returned 409 `batch contains non-test account`; the test account still existed afterward.
- Admin overview and account list loaded after the failed delete attempt.
- New child profile creation passed and handoff page showed the child joined.
- Assessment passed: generated first ability sketch, displayed ability scores, `+20 XP`, and child reaction controls.
- Sentence training passed: displayed AI coach feedback, `+25 XP`, `first_sentence_upgrade`, and saved child reaction.
- Essay draft feedback passed and enabled revision submission.
- Essay revision passed: displayed second-draft comparison, `+60 XP`, `first_revision`, and saved child reaction.
- Parent summary opened and showed practice counts, ability changes, highlights, next-step recommendation, and parent feedback controls.
- Parent summary feedback saved with `有帮助` pressed.
- Reading page stayed a controlled placeholder with `阅读峡谷施工中`.
- Parent report page showed controlled fallback `阶段报告暂时无法生成，请稍后再试。`, not a server error page.
- Demo login still rendered demo child links `小宇`, `小晴`, `小川`, and `小禾`.
- No-cookie parent children API returned 401 `parent session required`.
- Wrong Admin token returned 403 `admin token required`.
- Admin family detail loaded and exposed only sanitized payload metadata keys: `child_count`, `path`, `reaction`, `status`, `target_id`, `target_type`, `task_type`, and `usefulness`; no child writing text or AI feedback body keys were present.

## Failed / Needs Fix

- Disabled-account verify message:
  - Expected: `账号暂不可用，请联系邀请人。`
  - Actual: `绑定失败，请稍后再试。`
  - Path: legacy family binding flow at `/alpha/start`.

- Re-enabled account login landing:
  - Expected: enabled account can log in with Dev Echo and land on `/parent/children`.
  - Actual: `/alpha/start` showed `绑定失败，请稍后再试。`.
  - Follow-up check: direct navigation to `/parent/children` succeeded, so the session was created but the legacy-bind continuation path misreported failure.

- Admin hard delete allowlisted test account:
  - Expected: single allowlisted test account delete succeeds and account disappears from accounts list.
  - Actual: `POST /api/admin/alpha/accounts/delete-test` returned `500 Internal Server Error`; account remained present.

- Phone state after re-enabled legacy-bind path:
  - Expected: previously bound phone remains shown as bound after re-login.
  - Actual: `/parent/children` showed unbound phone UI after re-login.
  - Admin overview still reported `phone_bound=true`, indicating a front-end/session payload inconsistency rather than confirmed data loss.

## Known Blocked / Skipped

- Real Magic Code email arrival remains skipped for Railway Dev while outbound SMTP reachability is unresolved.
- Full invalid Origin/Referer and non-JSON state-changing request matrix was not re-run in this browser pass beyond valid Admin state-change requests and the two basic no-cookie/wrong-token checks.

