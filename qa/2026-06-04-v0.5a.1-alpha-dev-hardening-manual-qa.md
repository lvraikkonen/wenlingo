# V0.5a.1 Alpha Dev Hardening Manual QA

Date: 2026-06-04
Environment: Railway Dev

## Run 2026-06-05

Status: blocked on Magic Code email delivery.

Tester notes:

- Railway Dev web URL: `https://wenlingo-web-production.up.railway.app/alpha/start`.
- Admin token entered manually in the browser by the operator.
- SMTP/Magic Code env was manually configured in Railway `wenlingo-api` before this run.
- Do not record raw invite codes or full test email addresses in this QA file.

## Checklist

- [x] Railway Dev `MAGIC_CODE_DEV_ECHO=false`. Operator confirmed Railway env was configured for SMTP delivery with dev echo disabled.
- [x] QQ SMTP env uses authorization code not password. Operator configured `SMTP_PASSWORD` per runbook as the QQ Mail authorization code.
- [ ] Magic Code email arrives. KNOWN BLOCK/SKIPPED: Railway outbound SMTP to QQ returned `OSError: [Error 101] Network is unreachable`; this run continued with `MAGIC_CODE_DEV_ECHO=true` after redeploy for non-real-email QA.
- [x] Admin generate 1 invite and raw code visible once. Generated QA revoke-path invite from `/admin/alpha`; one-time warning and raw `ALPHA-*` code were visible on page.
- [x] Generated raw invite code not stored in localStorage/sessionStorage. Browser-side storage inspection did not find the generated raw code; raw code was only observed in the one-time page display.
- [x] Admin cannot generate count=0 or count=21. Both invalid counts did not add an invite; generated list remained unchanged except for the existing one-time display.
- [x] Admin can revoke unused invite. QA revoke-path invite changed from `issued` to `revoked`.
- [x] Revoked invite cannot validate or create family. Reusing the revoked invite at `/alpha/start` showed `邀请码无效或已失效，请检查后再试。`; no code field appeared and continue remained disabled.
- [x] Consumed invite cannot be revoked. QA consume-path invite appeared as `consumed` and had no row-level `Revoke invite` action after family creation.
- [x] Account list masks email and shows active session count. `/admin/alpha` account list displayed masked emails such as `ce***@...`, `lv***@...`, and `1 active session`.
- [x] Disable revokes sessions and blocks Magic Code login. Admin disabled the QA account; account list changed to `0 active sessions` with `Enable account`, and `/parent/children` redirected back to `/alpha/start`.
- [x] Disabled request returns generic request message. Disabled-account Magic Code request entered the verification step instead of revealing account status.
- [ ] Disabled verify shows `账号暂不可用，请联系邀请人。`. FAIL: submitting dev echo code `123456` while the account was disabled showed `绑定失败，请检查验证码后再试。` instead of the expected disabled-account message.
- [x] Enable allows new Magic Code login. After re-enable, Magic Code verification created a valid session and direct navigation to `/parent/children` succeeded. UX defect: the legacy-bind page still showed `绑定失败，请检查验证码后再试。` before direct navigation proved the session was valid.
- [x] Demo login still works after admin account operations. Home page `进入家庭内测` called demo login successfully and rendered demo child links `小宇`, `小晴`, `小川`, and `小禾`.
- [x] Parent report shows content or controlled fallback, not server error page. `/parent/s1/report` rendered `阶段报告暂时无法生成，请稍后再试。` with no server error page.
- [x] `/children/<student_id>/reading` remains documented as known placeholder. `/children/s1/reading` rendered `阅读峡谷施工中` and controlled construction copy.

## Current Blocker

Observed failure:

- Step: `/alpha/start` with generated QA consume-path invite, display name filled, and operator-provided test email.
- Action: clicked `获取验证码`.
- Actual: page displayed `验证码发送失败，请稍后再试。`.
- Frontend state: no `6 位验证码` input appeared; `继续使用 Alpha` stayed disabled.
- Interpretation: frontend code shows this message after invite validation succeeds but Magic Code request/email delivery fails.
- Railway `wenlingo-api` log evidence: `SMTP email send failed: OSError`, followed by `POST /api/auth/magic-codes/request` returning `503 Service Unavailable`.
- Railway console SMTP connection test evidence: `OSError: [Error 101] Network is unreachable`.

Recommended next check:

- Confirm Railway plan/networking supports outbound SMTP for this service. If SMTP remains unavailable, use a transactional email provider with an HTTPS API or temporarily mark real-email Magic Code QA blocked for this Railway Dev environment.
- If SMTP is expected to be available on the plan, redeploy `wenlingo-api`, rerun a direct SMTP connectivity test from the Railway console, and provide the host/port reachability output to Railway support if it remains unreachable.

## Dev Echo Follow-Up 2026-06-06

Operator set `MAGIC_CODE_DEV_ECHO=true` in Railway Dev to continue non-real-email QA.

Observed follow-up:

- Step: `/alpha/start` with the same QA consume-path invite and a non-real test email.
- Action: clicked `获取验证码`.
- Actual: request stayed pending for roughly the SMTP timeout window, then page again displayed `验证码发送失败，请稍后再试。`.
- Expected with Dev Echo active: immediate success and visible `6 位验证码` input using dev code `123456`.
- Interpretation: the running `wenlingo-api` behind the current Dev URL is still not seeing `MAGIC_CODE_DEV_ECHO=true`, or the variable was set on the wrong Railway environment/service.

Next check:

- Confirm `MAGIC_CODE_DEV_ECHO=true` is set on `wenlingo-api`, not `wenlingo-web`.
- Confirm it is set in the exact Railway environment that serves `https://wenlingo-web-production.up.railway.app`.
- Redeploy/restart `wenlingo-api` after the variable change, then retry `/alpha/start`.

Follow-up after redeploy:

- Operator confirmed `wenlingo-api` redeploy completed with `MAGIC_CODE_DEV_ECHO=true`.
- `/alpha/start` accepted the QA consume-path invite and dev echo code `123456`.
- Result: browser redirected to `/parent/children`.
- `/parent/children` showed the new Alpha family page with no child profiles yet and a `创建孩子档案` link.
- Interpretation: non-real-email Dev Echo auth and family creation flow passed. Real-email arrival remains a known blocked/skipped item for this Railway plan/networking setup.

Admin account disable checks:

- `/admin/alpha` showed the QA consume-path invite as `consumed`, linked to the new family, and without a `Revoke invite` action.
- Admin disabled the QA account; the account row changed from `1 active session` to `0 active sessions` and displayed `Enable account`.
- Visiting `/parent/children` after disable redirected to `/alpha/start`, confirming the active session was revoked.
- Disabled-account Magic Code request reached the verification form, confirming the request step stayed generic.
- Disabled-account verify did not show the expected message. Actual UI message was `绑定失败，请检查验证码后再试。`. Source check: backend defines `账号暂不可用，请联系邀请人。`, but the `/alpha/start` legacy binding catch path currently maps verify failures to the generic binding failure message.
- Admin re-enabled the QA account; the account row changed back to `Disable account` with `0 active sessions`.
- UX observation: after clicking `Enable account`, the button did not visibly enter a disabled/loading state before the async refresh completed. This can make the admin action feel unacknowledged.
- After re-enable, submitting dev echo code `123456` on `/alpha/start` displayed `绑定失败，请检查验证码后再试。`; however, direct navigation to `/parent/children` succeeded and showed the family page. Interpretation: Magic Code verify/session creation passed, but the legacy-bind continuation path misreported failure after login.
- Phone bind UX observation: after saving a phone number, `/parent/children` displayed `已绑定 <masked phone>`, but the phone input and `保存手机号` button remained visible and editable. This is current implementation behavior, but it may be clearer to hide the input after binding or switch to an explicit edit/change-phone mode.

Remaining checklist follow-up:

- Demo login after admin account operations passed from `/`; demo child links rendered for `小宇`, `小晴`, `小川`, and `小禾`.
- Parent report check passed: `/parent/s1/report` rendered the controlled fallback `阶段报告暂时无法生成，请稍后再试。` instead of a server error page.
- Known reading placeholder check passed: `/children/s1/reading` rendered `阅读峡谷施工中` and construction copy.
