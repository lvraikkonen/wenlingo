# V0.5b Hotfix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship the Dev QA follow-up hotfix for existing-account login, manual Alpha account cleanup, and environment variable documentation.

**Architecture:** Keep the product change in the existing `/alpha/start` client page and current Magic Code APIs. Reuse the existing Admin test-account cleanup delete graph for the ops script, adding explicit real-email and production safeguards. Document runtime/build variables in one canonical docs page, with README pointing to it.

**Tech Stack:** Next.js client components, Vitest Testing Library, FastAPI/SQLModel ops services, pytest, Markdown docs.

---

### Task 1: Existing Account Login Mode

**Files:**
- Modify: `apps/web/src/app/alpha/start/page.tsx`
- Test: `apps/web/tests/alpha_start_auth.test.tsx`

- [ ] Add a failing test that renders `/alpha/start` with no auth session, switches to an existing-account mode, requests a Magic Code without validating an invite, verifies the code, and routes to `/parent/children` when `/api/auth/magic-codes/verify` returns a linked parent.
- [ ] Add a failing test that verifies an authenticated but unlinked account in existing-account mode shows a clear "no Alpha family" message and does not call `createAlphaParent`.
- [ ] Implement a small mode state on `AlphaStartPage`: `create_family` and `existing_account`.
- [ ] In existing-account mode, hide invite and display-name inputs, skip `validateAlphaInvite`, request Magic Code by email only, and after verify route linked sessions to `/parent/children`.
- [ ] Keep create-family and legacy-bind behavior unchanged.
- [ ] Run the focused web auth tests.

### Task 2: Manual Alpha Account Cleanup Ops Script

**Files:**
- Modify: `apps/api/app/services/admin_test_account_cleanup.py`
- Create: `apps/api/app/ops/cleanup_alpha_accounts.py`
- Test: `apps/api/tests/test_admin_alpha_delete_test_accounts.py`

- [ ] Add failing service tests that non-test real emails are rejected by default and accepted only when `allow_real_email=True` with the stricter confirmation text.
- [ ] Add a failing script smoke test that the ops script contains dry-run default behavior, `--allow-real-email`, the confirmation text, and an `ENVIRONMENT=production` guard.
- [ ] Extract the current delete graph into a reusable function that accepts `allow_real_email` and `confirm`.
- [ ] Preserve existing Admin API semantics by keeping `delete_test_accounts` as the safe wrapper.
- [ ] Implement `app.ops.cleanup_alpha_accounts` with `--email`, `--account-id`, `--allow-real-email`, `--dry-run`, and `--confirm "DELETE ALPHA ACCOUNT"`.
- [ ] Print masked emails and object counts only; do not print full emails in script output.
- [ ] Run focused API cleanup tests.

### Task 3: Environment Variable Documentation

**Files:**
- Create: `docs/env-vars.md`
- Modify: `README.md`

- [ ] Add a docs page grouping API and web environment variables by purpose: base service, CORS, Admin, Auth/session, Magic Code, email providers, LLM, limits/cost, legacy migration, and web build-time config.
- [ ] Mark each variable as required/optional, default value, Dev recommendation, secret status, and redeploy requirement.
- [ ] Update README with a short environment variable section linking to `docs/env-vars.md` and calling out common Dev/Railway misses: `NEXT_PUBLIC_API_BASE_URL`, `CORS_ALLOW_ORIGINS`, `AUTH_ALLOWED_ORIGINS`, `MAGIC_CODE_EMAIL_PROVIDER`, `MAGIC_CODE_DEV_ECHO`, and `LLM_DAILY_LIMIT_ENABLED`.

### Task 4: V0.5b Hotfix Scope Docs

**Files:**
- Modify: `plans/2026-06-08-v0.5b-ai-infra-sentence-training-implementation.md`
- Modify: `qa/2026-06-11-v0.5b-ai-sentence-training-dev-manual-qa.md`

- [ ] Add a `V0.5b Hotfix Scope` section to the implementation plan covering the three follow-up changes.
- [ ] Add a QA follow-up validation checklist for existing-account login, cleanup dry-run/real-email guard, env docs, and Daily limit after `LLM_DAILY_LIMIT_ENABLED=true`.
- [ ] Keep the original Dev QA pass/skipped status intact; record the hotfix as follow-up scope.

### Task 5: Verification

**Files:**
- No new files.

- [ ] Run focused web tests for `alpha_start_auth`.
- [ ] Run focused API tests for cleanup behavior.
- [ ] Run a sensitive-string scan on the QA doc and env docs to ensure no raw invite codes, Magic Codes, Admin token, or full QA emails were recorded.
- [ ] Inspect `git diff` for scope.
